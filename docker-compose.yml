version: "3.8"

services:
  # ─── Telemetry Proxy ───
  proxy:
    build:
      context: .
      dockerfile: proxy/Dockerfile
    ports:
      - "8082:8082"
    environment:
      - PROXY_HOST=0.0.0.0
      - PROXY_PORT=8082
      - UPSTREAM_URL=http://host.docker.internal:3000
      - OBSERVABILITY_ENDPOINT=http://core:8001
      - PAYLOAD_STRATEGY=masked
      - GATEWAY_NAME=one-api-proxy
    depends_on:
      - core
    restart: unless-stopped

  # ─── Observability Core (FastAPI + SQLite) ───
  core:
    build:
      context: ./core
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      - DB_PATH=/data/observability.db
    volumes:
      - obs-data:/data
    restart: unless-stopped

  # ─── Web UI (React → nginx) ───
  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - core
    restart: unless-stopped

volumes:
  obs-data:
